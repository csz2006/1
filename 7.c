#include<stdio.h>
int main()
{
    int a,b;
    int i,n;
    scanf("%d%d",&a,&b);
    for(i=1;i<=a&&i<=b;i++){
        if(a%i==0){
            if(b%i==0){n=i;
            }else;}
        else; 
    }
    printf("%d",n);
    return 0;
}